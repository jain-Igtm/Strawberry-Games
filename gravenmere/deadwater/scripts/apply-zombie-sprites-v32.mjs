import { readFileSync, writeFileSync } from 'node:fs'
import { resolve } from 'node:path'

const root = resolve(import.meta.dirname, '..')
const mainPath = resolve(root, 'src/main.ts')
const zombieModelPath = resolve(root, 'src/zombie-model.ts')
const marker = '// DEADWATER_USER_AUTHORED_SPRITE_ZOMBIES_V32'

const zombieModel = "import * as THREE from 'three'\nimport { ZOMBIE_SPRITE_ATLAS_WEBP_V32 } from './generated-zombie-sprites-v32'\n\nexport type ZombieAnimationState = 'walk' | 'run' | 'attack' | 'hit' | 'death'\n\nexport const ZOMBIE_DISPLAY_HEIGHT = 2.34\nexport const ZOMBIE_FORWARD_YAW = -Math.PI / 2\nexport const ZOMBIE_MESHES_PER_VISUAL = 1\n\ntype ZombiePose = 'alive' | 'hit' | 'dying-one' | 'dying-two' | 'dead'\n\nexport type ZombieVisual = {\n  group: THREE.Group\n  parts: THREE.Mesh[]\n  mesh: THREE.Mesh<THREE.PlaneGeometry, THREE.MeshStandardMaterial>\n  billboard: THREE.Group\n  animationState: ZombieAnimationState\n  resumeState: Exclude<ZombieAnimationState, 'hit' | 'death'>\n  animationTime: number\n  hitTimer: number\n  deathProgress: number\n  currentPose: ZombiePose\n  disposed: boolean\n}\n\nlet sharedAtlas: THREE.Texture | null = null\nlet sharedMaterial: THREE.MeshStandardMaterial | null = null\nlet sharedGeometries: Record<ZombiePose, THREE.PlaneGeometry> | null = null\nlet assetFailed = false\n\nconst poseMetrics: Record<ZombiePose, { width: number; height: number; y: number }> = {\n  alive: { width: 1.46, height: 2.34, y: 1.17 },\n  hit: { width: 1.48, height: 2.34, y: 1.17 },\n  'dying-one': { width: 2.18, height: 2.01, y: 1.005 },\n  'dying-two': { width: 2.30, height: 1.87, y: 0.935 },\n  dead: { width: 2.62, height: 1.07, y: 0.535 },\n}\n\nconst atlasCells: Record<ZombiePose, { column: number; rowFromBottom: number }> = {\n  alive: { column: 0, rowFromBottom: 1 },\n  hit: { column: 1, rowFromBottom: 1 },\n  'dying-one': { column: 2, rowFromBottom: 1 },\n  'dying-two': { column: 0, rowFromBottom: 0 },\n  dead: { column: 1, rowFromBottom: 0 },\n}\n\nfunction ensureAtlas(): THREE.Texture {\n  if (sharedAtlas) return sharedAtlas\n  const loader = new THREE.TextureLoader()\n  sharedAtlas = loader.load(\n    ZOMBIE_SPRITE_ATLAS_WEBP_V32,\n    (texture) => {\n      texture.colorSpace = THREE.SRGBColorSpace\n      texture.minFilter = THREE.LinearMipmapLinearFilter\n      texture.magFilter = THREE.LinearFilter\n      texture.generateMipmaps = true\n      texture.anisotropy = 2\n      texture.needsUpdate = true\n    },\n    undefined,\n    () => { assetFailed = true },\n  )\n  sharedAtlas.colorSpace = THREE.SRGBColorSpace\n  sharedAtlas.minFilter = THREE.LinearMipmapLinearFilter\n  sharedAtlas.magFilter = THREE.LinearFilter\n  sharedAtlas.generateMipmaps = true\n  return sharedAtlas\n}\n\nfunction ensureMaterial(): THREE.MeshStandardMaterial {\n  if (sharedMaterial) return sharedMaterial\n  sharedMaterial = new THREE.MeshStandardMaterial({\n    map: ensureAtlas(),\n    color: 0xffffff,\n    transparent: true,\n    alphaTest: 0.15,\n    depthWrite: true,\n    side: THREE.DoubleSide,\n    fog: true,\n    roughness: 1,\n    metalness: 0,\n    emissive: 0x080a0a,\n    emissiveIntensity: 0.18,\n    toneMapped: false,\n  })\n  return sharedMaterial\n}\n\nfunction geometryForCell(column: number, rowFromBottom: number): THREE.PlaneGeometry {\n  const geometry = new THREE.PlaneGeometry(1, 1)\n  const uv = geometry.getAttribute('uv')\n  const u0 = column / 3\n  const u1 = (column + 1) / 3\n  const v0 = rowFromBottom / 2\n  const v1 = (rowFromBottom + 1) / 2\n  for (let index = 0; index < uv.count; index += 1) {\n    uv.setXY(\n      index,\n      THREE.MathUtils.lerp(u0, u1, uv.getX(index)),\n      THREE.MathUtils.lerp(v0, v1, uv.getY(index)),\n    )\n  }\n  uv.needsUpdate = true\n  return geometry\n}\n\nfunction ensureGeometries(): Record<ZombiePose, THREE.PlaneGeometry> {\n  if (sharedGeometries) return sharedGeometries\n  sharedGeometries = {\n    alive: geometryForCell(atlasCells.alive.column, atlasCells.alive.rowFromBottom),\n    hit: geometryForCell(atlasCells.hit.column, atlasCells.hit.rowFromBottom),\n    'dying-one': geometryForCell(atlasCells['dying-one'].column, atlasCells['dying-one'].rowFromBottom),\n    'dying-two': geometryForCell(atlasCells['dying-two'].column, atlasCells['dying-two'].rowFromBottom),\n    dead: geometryForCell(atlasCells.dead.column, atlasCells.dead.rowFromBottom),\n  }\n  return sharedGeometries\n}\n\nfunction applyPose(visual: ZombieVisual, pose: ZombiePose): void {\n  if (visual.currentPose === pose) return\n  visual.currentPose = pose\n  visual.mesh.geometry = ensureGeometries()[pose]\n  const metrics = poseMetrics[pose]\n  visual.mesh.scale.set(metrics.width, metrics.height, 1)\n  visual.mesh.position.set(0, metrics.y, 0)\n}\n\nexport const zombieAssetReady: Promise<boolean> = Promise.resolve(true)\n\nexport function isZombieAssetReady(): boolean {\n  return true\n}\n\nexport function didZombieAssetFail(): boolean {\n  return assetFailed\n}\n\nexport function createTexturedZombieVisual(): ZombieVisual {\n  const group = new THREE.Group()\n  group.name = 'user-authored-tall-zombie-sprite-v32'\n  group.userData.flashActive = false\n\n  const billboard = new THREE.Group()\n  billboard.name = 'zombie-camera-facing-billboard'\n  group.add(billboard)\n\n  const mesh = new THREE.Mesh(ensureGeometries().alive, ensureMaterial())\n  mesh.name = 'single-draw-call-zombie-sprite'\n  mesh.castShadow = false\n  mesh.receiveShadow = false\n  mesh.frustumCulled = true\n  mesh.renderOrder = 4\n  billboard.add(mesh)\n\n  // One plane always faces the camera. The navigation group may still rotate\n  // normally, while this inner billboard cancels that rotation for visibility.\n  const parentWorld = new THREE.Quaternion()\n  mesh.onBeforeRender = (_renderer, _scene, camera) => {\n    group.getWorldQuaternion(parentWorld)\n    billboard.quaternion.copy(parentWorld).invert().multiply(camera.quaternion)\n  }\n\n  const initialState: 'walk' | 'run' = Math.random() < 0.32 ? 'run' : 'walk'\n  const visual: ZombieVisual = {\n    group,\n    parts: [mesh],\n    mesh,\n    billboard,\n    animationState: initialState,\n    resumeState: initialState,\n    animationTime: Math.random() * Math.PI * 2,\n    hitTimer: 0,\n    deathProgress: 0,\n    currentPose: 'dead',\n    disposed: false,\n  }\n  applyPose(visual, 'alive')\n  group.rotation.y = ZOMBIE_FORWARD_YAW\n  return visual\n}\n\nexport function setZombieAnimation(\n  visual: ZombieVisual,\n  next: ZombieAnimationState,\n  _playbackRate = 1,\n): void {\n  if (visual.disposed || visual.animationState === 'death') return\n  if (visual.hitTimer > 0) {\n    if (next === 'walk' || next === 'run' || next === 'attack') visual.resumeState = next\n    return\n  }\n  visual.animationState = next\n  if (next === 'walk' || next === 'run' || next === 'attack') visual.resumeState = next\n}\n\nexport function showZombieHit(visual: ZombieVisual): void {\n  if (visual.disposed || visual.animationState === 'death') return\n  if (\n    visual.animationState === 'walk' ||\n    visual.animationState === 'run' ||\n    visual.animationState === 'attack'\n  ) {\n    visual.resumeState = visual.animationState\n  }\n  visual.animationState = 'hit'\n  visual.hitTimer = 0.14\n  applyPose(visual, 'hit')\n}\n\nexport function beginZombieDeath(visual: ZombieVisual): void {\n  if (visual.disposed) return\n  visual.animationState = 'death'\n  visual.hitTimer = 0\n  visual.deathProgress = 0\n  applyPose(visual, 'dying-one')\n}\n\nexport function advanceZombieAnimation(\n  visual: ZombieVisual,\n  dt: number,\n  _distanceToPlayer: number,\n): void {\n  if (visual.disposed) return\n  visual.animationTime += dt\n  const mesh = visual.mesh\n\n  if (visual.animationState === 'death') {\n    visual.deathProgress += dt\n    if (visual.deathProgress < 0.22) applyPose(visual, 'dying-one')\n    else if (visual.deathProgress < 0.52) applyPose(visual, 'dying-two')\n    else applyPose(visual, 'dead')\n\n    const settle = Math.min(1, visual.deathProgress / 0.58)\n    mesh.rotation.z = Math.sin(settle * Math.PI) * -0.025\n    mesh.position.z = -settle * 0.015\n    return\n  }\n\n  if (visual.animationState === 'hit') {\n    visual.hitTimer = Math.max(0, visual.hitTimer - dt)\n    applyPose(visual, 'hit')\n    mesh.rotation.z = -0.025 * (visual.hitTimer / 0.14)\n    if (visual.hitTimer <= 0) {\n      visual.animationState = visual.resumeState\n      applyPose(visual, 'alive')\n    }\n    return\n  }\n\n  applyPose(visual, 'alive')\n  const running = visual.animationState === 'run'\n  const attacking = visual.animationState === 'attack'\n  const rate = running ? 8.2 : attacking ? 5.4 : 5.9\n  const stride = Math.sin(visual.animationTime * rate)\n  const lift = Math.abs(Math.sin(visual.animationTime * rate * 0.5))\n\n  // Smooth, low-amplitude gait from the authored pose. This keeps the figure\n  // natural without adding skeletal animation, extra meshes, or extra draws.\n  mesh.rotation.z = stride * (running ? 0.026 : 0.018)\n  mesh.rotation.x = attacking ? -0.035 : 0\n  mesh.position.x = stride * (running ? 0.018 : 0.011)\n  mesh.position.y = poseMetrics.alive.y + lift * (running ? 0.038 : 0.024)\n  mesh.position.z = attacking ? -0.025 : 0\n  mesh.scale.set(\n    poseMetrics.alive.width * (1 - lift * 0.012),\n    poseMetrics.alive.height * (1 + lift * 0.01),\n    1,\n  )\n}\n\nexport function disposeZombieVisual(visual: ZombieVisual): void {\n  if (visual.disposed) return\n  visual.disposed = true\n  visual.mesh.onBeforeRender = () => undefined\n  // Atlas, material, and five UV geometries remain shared for the full session.\n}\n"

if (readFileSync(zombieModelPath, 'utf8') !== zombieModel) {
  writeFileSync(zombieModelPath, zombieModel)
}

let main = readFileSync(mainPath, 'utf8')
if (!main.includes(marker)) {
  const importBefore = '  beginZombieDeath,\n  createTexturedZombieVisual,'
  const importAfter = '  beginZombieDeath,\n  showZombieHit,\n  createTexturedZombieVisual,'
  if (!main.includes(importBefore)) {
    throw new Error('Could not add the sprite hit-state import after the v31 overhaul')
  }
  main = main.replace(importBefore, importAfter)

  const hitBefore = '    zombie.flashTimer = 0.075\n    const killed = zombie.health <= 0'
  const hitAfter = '    zombie.flashTimer = 0.075\n    showZombieHit(zombie.visual)\n    const killed = zombie.health <= 0'
  if (!main.includes(hitBefore)) {
    throw new Error('Could not connect weapon hits to the authored sprite hit pose')
  }
  main = main.replace(hitBefore, hitAfter)

  const runtimeAnchor = 'let zombieMoanTimer = 0\n'
  if (!main.includes(runtimeAnchor)) {
    throw new Error('Could not locate the v31 zombie runtime marker')
  }
  main = main.replace(runtimeAnchor, runtimeAnchor + marker + '\n')
  writeFileSync(mainPath, main)
  console.log('Installed the user-authored five-pose zombie sprite atlas.')
} else {
  console.log('The user-authored zombie sprite pass is already applied.')
}
